// SPA mode — disable SSR and prerender at the layout level.
// All pages are client-side rendered (required for adapter-static with dynamic routing).
export const prerender = true;
export const ssr = false;
